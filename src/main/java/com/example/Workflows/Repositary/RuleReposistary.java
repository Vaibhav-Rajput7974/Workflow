package com.example.Workflows.Repositary;

import com.example.Workflows.Entity.Rule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RuleReposistary extends JpaRepository<Rule,Long> {
}
